[agents]
max_threads = 4
max_depth = 1
job_max_runtime_seconds = 1800

# job_max_runtime_seconds is primarily the default runtime for
# spawn_agents_on_csv-style batch workers, not a universal timeout
# for every normally spawned subagent.
